const chai = require('chai');
const expect = chai.expect;
import { FinalScreen } from '../components/FinalScreen';

describe('FinalScreen', () => {
  let finalScreen: FinalScreen = null;

  beforeEach(() => {
    finalScreen = new FinalScreen();
  });

  describe('#draw', () => {
    it('should draw on canvas');
  });

});
